# Core framework
flask
flask_sqlalchemy

# Template engine
jinja2
werkzeug
itsdangerous
click
blinker

# --------------------------
# Database Drivers (Optional)
# --------------------------

# MySQL (only if you want MySQL DB)
flask-mysqldb ; extra == "mysql"
mysqlclient ; extra == "mysql"

# PostgreSQL (only if you want PostgreSQL DB)
psycopg2-binary ; extra == "postgres"

# SQLite → built-in (no extra install needed)

Flask==3.1.2
Flask-MySQLdb==2.0.0
gunicorn==21.2.0

